using System;

namespace FbxSharp
{
    public static class PropertyFlags
    {
        public enum EInheritType
        {
            eOverride,
            eInherit,
            eDeleted,
        }
    }
}

